# Bachat — Next.js app

See the [project README](../README.md) at the repository root for full setup instructions, API key guide, and feature documentation.

## Quick start

```bash
# From this directory (bachat/)
cp .env.example .env.local   # then fill in your API keys
npm install
npm run dev
```

Open `http://localhost:3000`.
